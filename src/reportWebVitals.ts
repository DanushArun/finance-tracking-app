const reportWebVitals = (onPerfEntry?: any) => {
  // Web vitals reporting is optional and can be safely disabled
  // if (onPerfEntry && onPerfEntry instanceof Function) {
  //   // Web vitals functionality would go here
  // }
};